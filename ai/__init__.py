"""AI layer.

- ``recommendation.py`` (Phase 8) — rule-based helper used by
  ``backend/app/services/recommendation_service.py``
- ``chatbot.py`` (Phase 9)         — demo/external chatbot provider used by
  ``backend/app/services/chatbot_service.py``
- ``prompts/``                     — prompt templates (Phase 9)

The provider is selected by the ``AI_PROVIDER`` environment variable.
Never place API keys in frontend code.
"""